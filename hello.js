function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('SEI 1019'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('bob'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('tim'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('hellen'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('mary'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

